==================== 情况说明 0629（单 Go2 在线视觉感知 → 多狗复用追踪）====================

本日目标：把“目标状态获取”从先验轨迹输入（直接读 Gazebo 真值 /walking_target/odom），
改为“在线视觉感知估计”。先在单只 go2_1 上跑通 RGB-D 感知最小闭环，再让多狗复用现有
三段式追踪策略。

================================ 一、最终方案思路 ================================

总思路：单狗感知估计 + 多狗复用现有追踪策略。

1. 单狗感知估计（本阶段重点）：
   - go2_1 挂 RGB-D 相机，用 YOLO 在 RGB 图里检测行人(person)，结合深度图把目标反投影成
     相机系下的 3D 相对位置，再经 TF 转到全局系（go2_1/odom ≈ 世界系，因用 ground-truth odom）。
   - 把估计结果发布成两路：
       /go2_1/target_pose_estimated   (geometry_msgs/PoseStamped, 全局系)
       /go2_1/target_estimated/odom   (nav_msgs/Odometry, 全局系 pose + 相邻帧差分速度)
   - 速度用相邻帧位置差分 + 一阶低通（复用 actor_state_publisher 的测速写法）。

2. 多狗复用现有追踪策略（后续阶段，本日未做）：
   - 关键设计：感知节点输出的 /go2_1/target_estimated/odom，其字段与
     actor_state_publisher 发布的真值 /walking_target/odom 完全同构。
   - 因此把 dynamic_encircle.py 里订阅的 /walking_target/odom 换成各狗（或融合后）的在线估计
     话题，三段式（approach → catch_up → formation）追踪/围捕逻辑几乎不用改，即可从“真值”
     平滑切换到“在线估计”。这就是“单狗感知 + 多狗复用”的衔接点。

============================ 二、目前的最小闭环方案 ============================

范围：单只 go2_1，做到“识别 → 测距测速 → 发布全局估计”，本日不接控制器（不驱动狗）。

1) 仿真与传感器
   - 世界：QY_MODEL/target_seek（含 <actor name="walking_target"> 行人）。
   - 相机：原地把 go2_description/xacro/camera_rgb_3d.xacro 从纯 RGB 升级为 RGB-D
     （<sensor type="depth">），保留多狗命名空间技巧（camera_name 用绝对名
     $(arg robot_namespace)/camera，frame_name 用 frame_prefix，plugin 名带 name_suffix）。
   - 接线沿用现有 enable_camera 开关（robot_3d_lidar_nocam.xacro 第 104-106 行无需改）；
     三个 spawn_go2_velodyne_{1,2,3}.launch.py 的 enable_camera 默认改为 true。
   - 相机参数：640x480，update_rate=15，clip near=0.05 / far=50（far 起初为 10 会导致远处
     建筑看不见，已调到 50），plugin min_depth=0.05 / max_depth=10。
   - 产出话题：/go2_1/camera/image_raw、/camera_info、/depth/image_raw(32FC1 米)、
     /depth/camera_info、/points。

2) 感知节点 multi_go2_waypoint/target_perception.py（核心）
   流程：
   a. message_filters.ApproximateTimeSynchronizer 同步 RGB + Depth（slop 可调）。
   b. cv_bridge 转 bgr8 / 32FC1；从 depth/camera_info 取内参 fx,fy,cx,cy。
   c. YOLO 检测 person（classes=[0]），选面积最大的一个 bbox。
   d. 取 bbox 中心 (u,v)，在中心小窗口内对 depth 取中值得 Z（抗空洞/噪声）；
      Z 无效（NaN/0/超 max_depth）则丢弃该帧。
   e. 反投影到 camera_depth_optical_frame：X=(u-cx)*Z/fx, Y=(v-cy)*Z/fy, Z=Z。
   f. tf2 把 PointStamped 转到目标系 go2_1/odom（异常回退到最新可用变换）。
   g. 发布 PoseStamped + Odometry（速度=差分+低通）。
   h. 成功时每 ~0.5s 在终端节流打印估计 odom（x/y/距离Z/conf/vx/vy），兼作轻量诊断。
   i. 发布带框调试图 /go2_1/target_perception/debug_image（QoS=sensor_data，与 image_raw
      一致，保证 rqt_image_view 能正常显示；RViz/rqt 均可看框）。
   可调参数：model_path、conf、imgsz、device、depth_window、max_depth/min_depth、
            sync_slop、vel_lpf_alpha、tf_timeout、publish_debug。

3) 误差评估节点 multi_go2_waypoint/perception_eval.py
   - 订阅 /go2_1/target_estimated/odom（估计）与 /walking_target/odom（真值 baseline）。
   - 估计帧到来时取时间最近的真值配对（match_timeout 内），算位置误差 ex/ey/欧氏距离 e、
     速度误差，并维护 mean/RMSE/max/n，按 log_period 节流打印；可选发 /go2_1/perception_error。

4) 依赖与构建
   - pip 安装 ultralytics（系统 python /usr/bin/python3）。注意它会顺带升级 numpy 到 2.x，
     会让 cv_bridge 的 C 扩展崩（_ARRAY_API not found），已把 numpy 降回 1.26.4 修复。
   - setup.py 注册 target_perception / perception_eval；package.xml 增加 sensor_msgs /
     std_msgs / cv_bridge / image_geometry / tf2_ros / tf2_geometry_msgs / message_filters。
   - colcon build --symlink-install（go2_description / go2_config / multi_go2_waypoint）。
     之后改 .py 即时生效，无需再 build。

5) 启动顺序（每个终端先 conda deactivate，确认 which python3=/usr/bin/python3）
   终端1: ros2 launch go2_config gazebo_target_seek_world.launch.py gui:=true
   终端2: ros2 launch go2_config spawn_go2_velodyne_1.launch.py        # 现在默认带 RGB-D
   终端3: ros2 run multi_go2_waypoint actor_state_publisher --ros-args -p use_sim_time:=true
   终端4: cd /home/bit/go2_target_seek_delivery &&                      # 模型为相对路径
          ros2 run multi_go2_waypoint target_perception --ros-args -p use_sim_time:=true
   终端5: ros2 run multi_go2_waypoint perception_eval --ros-args -p use_sim_time:=true

6) 已验证
   - 相机 RGB/Depth/CameraInfo 均为 640x480，三者一致。
   - 离线在 Figure/person1~3.png 上 YOLO 对 person 置信度 0.89~0.91，识别本身没问题。
   - 节点终端持续打印估计 odom；rqt 能看到带框调试图；估计 odom 与真值可由 perception_eval 对比。

============================ 三、目前遇到的问题 ============================

1) 静态识别率偏低（远/小目标）
   - 现象：行人离得近时识别尚可，但稍远（如 Z≈6.4m）或目标在画面里偏小时，置信度掉到
     0.40 上下，频繁跌破阈值 → 漏检。近处置信度也只有 0.49~0.66，明显低于离线大图的 0.9。
   - 原因分析：
     a. 默认用 yolov8n（nano，最小模型），对小/远目标召回最弱；
     b. 实时相机仅 640x480，目标一远像素就很少；
     c. 场景里行人是张臂 T-pose 等非常规姿态，COCO 预训练本就给低分；
     d. 初版 conf=0.4 偏高，把远处 0.3~0.4 的真检测滤掉了；
     e. 场景存在多个人形（person_standing / person_standing_0 / walking_target），
        “选最大框”在多目标下不稳。

2) 运动抖动 / 漏检（狗一动就丢）
   - 现象：go2_1 一旦运动起来，识别就时有时无，估计 odom 断断续续。
   - 原因分析：相机随狗运动 → 画面低分辨率混叠（类“运动模糊”）+ 目标尺度变化，逐帧独立
     检测没有时序记忆，单帧一旦失败估计就中断，缺少“跨帧维持 + 运动预测/coast”机制。

3) 本日已做的“轻量缓解”（先看召回提升多少，尚未上跟踪/滤波）
   - 模型默认 yolov8n → yolov8m；conf 0.4 → 0.25；新增 imgsz=960（上采样提升小目标召回）。
   - 这些都是参数化，可现场切：嫌慢用 yolov8s 或 imgsz=640；想更激进 conf=0.15。

============================ 四、主流解决方案（后续可上） ============================

针对“一帧检到一帧丢 + 运动抖动”，业界可靠做法分三层（从便宜到稳）：

第 1 层 检测器召回（最便宜、收益最大）——本日已做轻量版
   - 用更强模型（yolov8m/l 替 nano）；降 conf 阈值；提高推理 imgsz；必要时提相机分辨率。

第 2 层 时序多目标跟踪（核心，开源标准方案）
   - ByteTrack（ECCV2022）：核心是“用低分框维持轨迹”，专治检测闪烁；ultralytics 自带
     bytetrack.yaml，model.track(persist=True) 即可（需 pip install lapx）。
   - BoT-SORT：在 ByteTrack 上加相机运动补偿(GMC)。本场景“狗动=相机动”，BoT-SORT 通常比
     ByteTrack 更稳，推荐优先。ultralytics 自带 botsort.yaml。
   - 还有 OC-SORT / DeepOCSORT，对遮挡/非线性运动更鲁棒。
   - 配合“目标锁定”：用轨迹 ID + 离上一帧估计位置最近的人，锁定 walking_target，避免在
     多个人形间跳。

第 3 层 运动模型 + 短时 coast（让估计连续平滑，给下游控制铺路）
   - 在世界系对目标位置加恒速卡尔曼滤波（CV 模型，numpy/scipy 手写，不必依赖 filterpy）：
     有检测就 update，无检测就 predict。
   - coast：短暂漏检时继续按预测发布最多 coast_timeout（如 0.6s）再停，避免 odom 断流，
     估计更平滑。这正是各类 SOTA 跟踪器内部都用的运动模型思想。

环境就绪情况：ultralytics 自带 bytetrack/botsort/ocsort/deepocsort 配置已在；scipy 已装；
仅缺 lap/lapx（上 ByteTrack/BoT-SORT 时 pip 装 lapx 即可），filterpy 可不装（卡尔曼手写）。

后续衔接：感知输出已与真值同构，待召回/稳定性达标后，把 dynamic_encircle.py 的
/walking_target/odom 订阅切到在线估计话题，即可让多狗三段式围捕在“未知轨迹 + 在线感知”
下运行。
