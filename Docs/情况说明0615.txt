====================================今日改动 0615===========================

0. 修复（承接 0612 弯路还原）：恢复单狗 2D 模式启动
   现象：
      - ros2 launch go2_config gazebo_target_seek_2d_lidar.launch.py 报
        "error: Undefined substitution argument name_suffix"（gazebo.xacro 内）。
   根因：
      - 0612 调试时给 gazebo.xacro 三个插件名加了 _$(arg name_suffix)，但单狗
        robot_2d_lidar.xacro 没有声明该 arg，xacro 展开即失败。name_suffix 本就是 0612
        记录的“弯路”（能防 FQN 重名但没解决错位，真修复是 spawn 的 -robot_namespace），冗余。
   改动：
      - gazebo.xacro 三处插件名还原为 git 初始基线的无后缀名：
        p3d_base_controller / gazebo_ros2_control / imu_plugin。
      - velodyne_3d.xacro 自带的 name_suffix 不动；3D spawn 仍传 name_suffix，无害冗余。


